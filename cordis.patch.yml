# Bundle wiring for dsh plugin add / registry installs.
- insert:
    - id: claude-code
      name: dsh-claude-code
      config:
        model: sonnet
        permissionMode: acceptEdits
        maxTurns: 100
        timeoutMs: 600000
