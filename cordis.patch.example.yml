# dsh-claude-code 自带 bundle 接线（仓库根 cordis.patch.yml）。
# 手动安装时，把这段 insert 合并进 profile 的 cordis.patch.yml：
- insert:
    - id: claude-code
      name: dsh-claude-code
      config:
        model: sonnet
        permissionMode: acceptEdits
        maxTurns: 100
        timeoutMs: 600000
